/**
 * CaliTrack Design System — Theme barrel export
 *
 * Importa desde aquí todo lo que necesites del sistema de diseño:
 *   import { tokens, colors, motion, zIndex } from '@/theme'
 */
export { tokens, colors, typography, spacing, radii, shadows, zIndex, motion, breakpoints } from './tokens'
export type { Tokens, ColorToken, MotionVariant } from './tokens'
